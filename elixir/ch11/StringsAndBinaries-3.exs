# Exercise: StringsAndBinaries-3
# Chapter 11, Page 123

# Run this in the IEx.
[ 'cat' | 'dog' ]

# IEx prints 'cat' as a string, but 'dog' as individual
# letters because 'cat' is an embedded list. Lists are
# unprintable, so the characters 'd', 'o', and 'g' are
# outputted as integers.
